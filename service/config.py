"""Application default config

to add values, define variables with capitalized name
"""


from pathlib import Path

# to use in paths relatives to application dir
here = Path(__file__).resolve().parent
